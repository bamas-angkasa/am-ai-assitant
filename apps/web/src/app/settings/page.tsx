"use client";

import { Bot, Building2, Clock3, DatabaseZap, KeyRound, LockKeyhole, ShieldCheck, UsersRound } from "lucide-react";
import { PageHeader } from "@/components/page/page-header";
import { SyncHealthBadge } from "@/components/status/badges";
import { Badge } from "@/components/ui/badge";
import { useMaintenance } from "@/lib/state/maintenance-provider";

export default function SettingsPage() {
  const { snapshot } = useMaintenance();
  return <div className="space-y-6"><PageHeader eyebrow="Workspace configuration" title="Settings" description="Review organization, integration, access, and Phase 1 safety configuration." /><div className="grid gap-5 xl:grid-cols-2"><SettingsCard icon={Building2} title="Organization" description="Current workspace and portfolio ownership"><Field label="Organization name" value="Vantage Property Management" /><Field label="Primary portfolio" value="Vantage Residence Apartment" /><Field label="Environment" value="Staging / stakeholder demo" /></SettingsCard><SettingsCard icon={DatabaseZap} title="AppFolio integration" description="Connection and synchronization settings" badge={<SyncHealthBadge status={snapshot.integration_health.appfolio_status} />}><Field label="Connection" value="AppFolio ·•••• 4821" /><Field label="Sync frequency" value="Every 5 minutes" icon={Clock3} /><Field label="Authentication" value="Managed secret" icon={KeyRound} /></SettingsCard><SettingsCard icon={Bot} title="AI recommendation provider" description="Draft generation configuration"><Field label="Provider" value="OpenAI-compatible provider" /><Field label="Mode" value="Structured recommendation output" /><Field label="Human review" value="Required for every draft" icon={ShieldCheck} /></SettingsCard><SettingsCard icon={UsersRound} title="Roles and access" description="Organization-scoped internal permissions"><Field label="Administrators" value="2 users" /><Field label="Property managers" value="5 users" /><Field label="Maintenance staff" value="8 users" /><p className="mt-3 text-xs leading-5 text-slate-400">Tenant, owner, and vendor accounts are not enabled in Phase 1.</p></SettingsCard></div><section className="surface-card overflow-hidden"><div className="border-b border-slate-100 p-5"><div className="flex items-center gap-2"><LockKeyhole className="h-4 w-4 text-indigo-600" /><h2 className="text-base font-semibold">Phase 1 safety rules</h2></div><p className="mt-1 text-sm text-slate-500">These controls are mandatory and cannot be disabled.</p></div><div className="grid gap-4 p-5 md:grid-cols-2">{["Manager approval is required", "Internal # notes are excluded", "Unsupported ETA and cost claims are flagged", "Every review action is audited"].map((rule) => <div key={rule} className="flex items-center justify-between rounded-2xl border border-slate-200 bg-slate-50/60 p-4"><div className="flex items-center gap-3"><span className="flex h-8 w-8 items-center justify-center rounded-xl bg-emerald-50 text-emerald-600"><ShieldCheck className="h-4 w-4" /></span><p className="text-sm font-semibold text-slate-700">{rule}</p></div><Badge variant="success">Enforced</Badge></div>)}<div className="flex items-center justify-between rounded-2xl border border-slate-200 bg-slate-100 p-4 opacity-75"><div><p className="text-sm font-semibold text-slate-700">Automatic outbound sending</p><p className="mt-1 text-xs text-slate-500">Not available in Phase 1</p></div><button type="button" disabled className="relative h-6 w-11 rounded-full bg-slate-300" aria-label="Automatic outbound sending disabled"><span className="absolute left-1 top-1 h-4 w-4 rounded-full bg-white" /></button></div></div></section></div>;
}

function SettingsCard({ icon: Icon, title, description, badge, children }: { icon: typeof Building2; title: string; description: string; badge?: React.ReactNode; children: React.ReactNode }) { return <section className="surface-card p-5"><div className="flex items-start justify-between gap-3"><div className="flex gap-3"><span className="flex h-10 w-10 items-center justify-center rounded-xl bg-indigo-50 text-indigo-600"><Icon className="h-5 w-5" /></span><div><h2 className="text-sm font-semibold text-slate-900">{title}</h2><p className="mt-1 text-xs text-slate-400">{description}</p></div></div>{badge}</div><div className="mt-5 divide-y divide-slate-100">{children}</div></section>; }
function Field({ label, value, icon: Icon }: { label: string; value: string; icon?: typeof Clock3 }) { return <div className="flex items-center justify-between gap-4 py-3 first:pt-0 last:pb-0"><p className="text-xs font-medium text-slate-400">{label}</p><p className="flex items-center gap-1.5 text-right text-xs font-semibold text-slate-700">{Icon && <Icon className="h-3.5 w-3.5 text-slate-400" />}{value}</p></div>; }
