# AppFolio AI Frontend

Next.js frontend workspace for the AppFolio AI maintenance assistant.

## Run

From the repository root:

```bash
npm run dev
```

Or from this workspace:

```bash
npm run dev
```

The app runs on `http://localhost:3000`.
